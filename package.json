{
  "name": "receiptor-app",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "check": "pnpm build && pnpm test"
  },
  "devDependencies": {
    "prisma": "^7.9.1"
  }
}
