#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/home/gamp/receiptor-app/node_modules/.pnpm/@stellar+stellar-sdk@16.2.0/node_modules/@stellar/stellar-sdk/node_modules:/home/gamp/receiptor-app/node_modules/.pnpm/@stellar+stellar-sdk@16.2.0/node_modules:/home/gamp/receiptor-app/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="/home/gamp/receiptor-app/node_modules/.pnpm/@stellar+stellar-sdk@16.2.0/node_modules/@stellar/stellar-sdk/node_modules:/home/gamp/receiptor-app/node_modules/.pnpm/@stellar+stellar-sdk@16.2.0/node_modules:/home/gamp/receiptor-app/node_modules/.pnpm/node_modules:$NODE_PATH"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../@stellar/stellar-sdk/bin/stellar-js" "$@"
else
  exec node  "$basedir/../@stellar/stellar-sdk/bin/stellar-js" "$@"
fi
